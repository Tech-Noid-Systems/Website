// JavaScript Document



/* Menu Styling (active menu items)------------------------- */
/*	$('.menu').each(function () {
		//Get all tabs
		var menuitems = $(this).children('li').children('a');
		menuitems.click(function (e) {
			$('.menu .active').removeClass('active');
			$(this).addClass('active');
		});
	});
*/